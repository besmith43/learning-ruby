<%= form_for(reference) do |f| %>
  <% if reference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reference.errors.count, "error") %> prohibited this reference from being saved:</h2>

      <ul>
      <% reference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :owner, :value => current_user.id %>
  </div>

  <div class="field">
    <%= f.label :reference_name %>
    <%= f.text_field :reference_name %>
  </div>

  <div class="field">
    <%= f.label :reference_contact_info %>
    <%= f.text_field :reference_contact_info %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
